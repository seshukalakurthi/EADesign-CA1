apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: ca1-ingress
  namespace: ca1
spec:
  ingressClassName: traefik
  rules:
    - http:
        paths:
          - path: /
            pathType: Prefix
            backend:
              service:
                name: gateway-svc
                port:
                  number: 80
